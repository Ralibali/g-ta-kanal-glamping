import { OverviewDashboard } from "./OverviewDashboard";

// Conversion dashboard now shares the same real analytics view
export const ConversionDashboard = () => {
  return <OverviewDashboard />;
};
